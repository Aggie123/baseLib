
/**
 * 1. object遍历
 * 1.1 Object.keys(obj).foarEach(key=>{})
 * 1.2 for(let key in obj){}
 * 1.3 Object.getOwnProperties().forEach(key=>{})
 for … in
Object.getOwnPropertyNames
Object.keys
for … of ES6
Object.values ES7
Object.entries ES7
 */


 /**
 * 1. Array遍历
 * 1.1 Object.keys(obj).foarEach(key=>{})
 * 1.2 for(let key in obj){}
 * 1.3 Object.getOwnProperties().forEach(key=>{})
 */
